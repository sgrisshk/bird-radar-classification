# Archive

This directory stores legacy local files that are not part of the active training pipeline.

Subfolders:
- `root_logs/` archived run logs moved from repository root.
- `root_submissions/` archived root-level submission CSV files.
- `root_scripts_legacy/` archived helper scripts kept for reference.

These files are not required for normal project usage and are excluded from version control where appropriate.

